server:
  port: 8081
  connection-timeout: 3000ms

spring:
  datasource:
    user:
      url: jdbc:mysql://127.0.0.1:3306/user?allowMultiQueries=true&Unicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: aszx
    product:
      url: jdbc:mysql://127.0.0.1:3306/product?allowMultiQueries=true&Unicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
      username: root
      password: aszx      
    type: com.alibaba.druid.pool.DruidDataSource
    driverClassName: com.mysql.jdbc.Driver
    initial-size: 10
    max-active: 100
    min-idle: 10
    max-wait: 60000
    pool-prepared-statements: true
    max-pool-prepared-statement-per-connection-size: 20
    time-between-eviction-runs-millis: 60000
    min-evictable-idle-time-millis: 300000
    validation-query: SELECT 1 FROM DUAL
    test-while-idle: true
    test-on-borrow: false
    test-on-return: false
    stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        allow: 10.150.129.*,127.0.0.1
    filter:
      stat:
        enabled: true
        log-slow-sql: true
        slow-sql-millis: 1000
        merge-sql: false
      wall:
        config:
          multi-statement-allow: true
          schedules: 3600000
          
defaultUserName: ls  