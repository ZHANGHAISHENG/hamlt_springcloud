spring:
  application:
    name: common-test
  cloud:
    nacos:
      discovery:
      config:
        # 加载项目配置
        file-extension: yaml
        # 加载全局配置
        extension-configs[0]:
          data-id: server.yaml
          refresh: true
        extension-configs[1]:
          data-id: redis.yaml
          refresh: true
        extension-configs[2]:
          data-id: rocketMq.yaml
          refresh: true





